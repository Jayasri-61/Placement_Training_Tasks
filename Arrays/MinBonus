package arrays;
import java.util.*;
public class ArrayMin{
    public static void main(String[] str) {
    	int[] num= {45,56,67,78};
    	for(int i=0;i<num.length;i++) {
    		num[i]=Math.max(50, num[i]-10);
    	}
    	for(int i:num) {
    	System.out.println(i); 	
    }
    }	
}
