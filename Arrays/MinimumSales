package arrays;
import java.util.Scanner;
public class MinimumSales {
	public static void main(String[] str) {
		int[] Sales= {23,45,67,89,90};
		int min=Integer.MAX_VALUE;
		for(int i=0;i<Sales.length;i++) {
			if(Sales[i]<min) {
				min=Sales[i];
			}
		}
			System.out.println(min);
		}
	}
